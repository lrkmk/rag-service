# ATRIP / 用户管理 — RAG 切片

对「01-ATRIP」下 1 篇源文章（`用户管理.md`）做的切片，沿用「04-售后票务/退票」示例的格式与原则。`ATRIP.md` 是导航/链接页，未纳入切片。

## 分类结构说明

01-ATRIP 是顶级分类，「用户管理」下没有更细的子分类，因此本文章的 `level1_category` 与 `level2_category` 均取值 `ATRIP` / `用户管理` 本身（与售后票务示例中"大类/子类"两层结构不同）。

## 文件

- `parents.jsonl` — 文章级 parent 记录（1 条）
- `children.jsonl` — 规则单元级 child 记录（5 条）

## 切分依据

1. **文章为最小一级边界**：全篇只有 1 篇源文章，产出 1 个 parent + 5 个 child。
2. **按功能小节切分，不按字数机械切断**：源文章本身用加粗文本充当小节标题（访问入口／用户列表／创建用户／更新用户信息），切片沿用这一结构；`um-c03`（字段说明）作为一个独立的字段词典单元完整保留，未拆散成逐字段的碎片。
3. **航司特例**：本文无航司特例内容，`applicable_carrier` 统一为 `通用`。
4. **Parent-Child 结构**：每个 child 都带 `parent_id`，可回溯到「用户管理」文章摘要。
5. **FAQ**：本文不含 FAQ 内容，未涉及。
6. **Metadata 字段**：与退票示例字段结构完全一致（`level1_category`/`level2_category`/`applicable_carrier`/`updated_at`/`source_path`）。源文章及其导航页 `ATRIP.md` 均未标注更新日期，`updated_at` 统一记为 `null`。
7. **数字类规则抽表**：本文不含大规模结构化表格（如航司时限表），无需额外的结构化 JSON 抽取。

## 判断说明

- "创建用户"一节原文仅说"输入名、姓、电子邮件地址和角色信息"，未使用 First Name/Last Name 等英文字段名，切片保留原文措辞，未做额外翻译或字段名发明。
- "用户列表"中"筛选功能"与"两类用户数据（手动创建/SSO登录）"合并为同一个 child（`um-c02`），因二者同属"列表功能说明"这一个语义单元，拆分意义不大。
